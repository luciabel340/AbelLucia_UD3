using UnityEngine;
using System.Collections;

public class T09 : MonoBehaviour {

	// Use this for initialization
	void Start () {

		/*

		// Operadores: +=, -=, *=, /=, %=
		int puntos = 0;
		// Las siguientes dos lineas hacen exactamente lo mismo
		puntos = puntos + 10;			
		puntos += 10;
		
		// Las siguientes dos lineas hacen exactamente lo mismo
		puntos = puntos * 10;
		puntos *= 10;
		
		// Operadores: ++, --
		int vidas = 0;
		//vidas = vidas - 1;
		//vidas -= 1;
		//--vidas; // vidas--;
		// vidas tendra una unidad menos
		//int a = ++vidas;
		//Debug.Log("A: "+a);
		//Debug.Log("Vidas: "+vidas);
		
		// Operador: ?:
		int edad = 17;
		int dificultad = (edad < 18) ? 10 : 50;
		
		string mensaje = (vidas <= 0) ? "Game Over" : "Todavia te queda vidas";
		Debug.Log(mensaje); 
		
		*/
	}

}
